# 📰 Controller: Guest Content (`Guest/Content`)

## 1. File Controller
- `ContentController.php`

## 2. Aktor yang Berkomunikasi
- **Tamu / Publik**: Membaca artikel blog (`/posts`), promo aktif (`/promos`), banner popup (`/popup/active`), galeri foto (`/gallery-items`), testimoni (`/testimonials`), dan tautan unduhan aplikasi mobile (`/download-apps`).

## 3. Arah & Alur Data
- **Request**: Endpoint publik konten.
- **Response**: JSON data konten terpublikasi.
